using UnityEngine;

public class FurnitureDataModel
{
    // non mono behaviour model, to allow serialization later on

    // position, bounds, label, etc, moveable and non moveable (doors & windows are nor moveable)
}
